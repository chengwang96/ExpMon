# Frontend collector endpoint. Override this when the collector runs on a custom port.
VITE_COLLECTOR_URL=http://127.0.0.1:5184

# Optional collector settings for local shells. These are read by scripts/local_collector.py.
EXPMON_COLLECTOR_PORT=5184
EXPMON_CONFIG=./expmon-local.yaml
